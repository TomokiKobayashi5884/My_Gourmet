<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-8 mb-5">
      <h4 class="my-3">パスワード再設定用メール送信</h4>
      
      <hr>
      
      <p>
        ご登録時のメールアドレスを入力してください。<br>
        パスワード再設定用のメールをお送りします。
      </p>
      
      <%= form_for resource, as: resource_name, url: password_path(resource_name), method: :post do |f| %>
          
        <div class="form-group mt-4">
          <%= f.email_field :email, class: "form-control #{ "is-invalid" if resource.errors.messages[:email].present? }",
            autofocus: "true", required: "required", autocomplete: "email", placeholder: "メールアドレス" %>
          <!--エラー発生時はエラーメッセージを表示-->
          <% if resource.errors.messages[:email].present? %>
            <span class="app-error-message">
              <storng><%= resource.errors.messages[:email].first %></storng>
            </span>
          <% end %>
        </div>
        
        <div class="form-group">
          <%= f.submit "送信", class: "btn btn-danger w-100" %>
        </div>
            
      <% end %>    
      
    </div>
  </div>
</div>

