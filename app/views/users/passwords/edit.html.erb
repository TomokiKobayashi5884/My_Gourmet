<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-8 mb-5">
      <h4 class="my-3">パスワード再設定</h4>
      
      <hr>
      
      <%= form_for resource, as: resource_name, url: password_path(resource_name), method: :put do |f| %>
          
        <div class="form-group mt-4">
          <!--パスワードリセットトークンを送信-->
          <%= f.hidden_field :reset_password_token %>
          <%= f.password_field :password, class: "form-control #{ "is-invalid" if resource.errors.messages[:password].present? }",
            autofocus: "true", required: "required", placeholder: "新しいパスワード" %>
            
          <!--エラー発生時はエラーメッセージを表示-->
          <% if resource.errors.messages[:name].any? %>
            <span class="invalid-feedback">
               <% resource.errors.full_messages_for(:name).each do |message| %>
                 <stronng><%= message %></stronng><br>
               <% end %>
            </span>
          <% end %>
        </div>
        
        <div class="formgroup mt-4">
          <%= f.password_field :password_confirmation, class: "form-control #{ "is-invalid" if resource.errors.messages[:password_confirmation].present? }",
             required: "required", placeholder: "新しいパスワード(確認用)" %>
        </div>
        
        <p>※パスワードは英数字を含む8文字以上で登録してください。</p>
        
        <div class="form-group">
          <%= f.submit "パスワードリセット", class: "btn btn-danger w-100" %>
        </div>
            
      <% end %>    
      
    </div>
  </div>
</div>
